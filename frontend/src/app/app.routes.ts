import { Route } from '@angular/router';
import { HomepageComponent } from '../screens/homepage.component';
export const appRoutes: Route[] = [
    {
        path : "",
        component: HomepageComponent
    }
];
